---
scope: rules directory
load_when: working with rule files
---

purpose: atomic rule files. AI loads only rules relevant to the current task.
patterns: one rule file per domain (doc-style, overlay-design, pvp-mechanics, ...). no cross-domain mixing.
convention: load_when frontmatter tells AI when each file is relevant.
